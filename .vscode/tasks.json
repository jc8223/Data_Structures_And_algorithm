{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\MinGW\\bin\\g++.exe",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    // clear the terminal before each run
    // any other option to clear the terminal
    
    "presentation": {
        "clear": true
    },
    //pre launch command clear the terminal before each run
    "preLaunchTask": "clear",
    

    "version": "2.0.0"
}